# Third-Party Headers

Place the headers for your third-party dependencies (GLFW, GLAD, Assimp, etc.) in this directory so that the Visual Studio project can resolve them via the relative include path `$(SolutionDir)..\includes`.
