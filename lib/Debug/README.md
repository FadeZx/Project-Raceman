# Debug Libraries

Copy the 64-bit debug libraries for the engine's third-party dependencies into this folder. The project expects files such as `glfw3.lib`, `assimp.lib`, `freetype.lib`, `glew32s.lib`, `irrKlang.lib`, and `SOIL.lib` to be present when building the Debug configuration.
