# Release Libraries

Copy the 64-bit release libraries for the engine's third-party dependencies into this folder. The Visual Studio project links against this directory when building the Release configuration.
